# Copy to /etc/minio.env on the server (mode 600). Do not commit the real file.
# systemd reads this via EnvironmentFile in deploy/minio.service.
# Bind stays 127.0.0.1 — do not open 9000/9001 on the firewall.

MINIO_ROOT_USER=change-me
MINIO_ROOT_PASSWORD=change-me-at-least-8-chars
